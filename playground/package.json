{
  "private": true,
  "name": "nuxt-i18n-next-playground",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "prepare": "nuxt prepare",
    "generate": "nuxi generate",
    "test": "vitest --config tests/vitest.unit-config.ts"
  },
  "devDependencies": {
    "nuxt-i18n-micro": "^1.37.0"
  }
}
