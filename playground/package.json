{
  "private": true,
  "name": "nuxt-i18n-next-playground",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "client:prepare": "nuxt prepare",
    "generate": "nuxi generate",
    "init": "nuxt prepare",
    "typecheck": "tsc --noEmit",
    "test:types": "vue-tsc --noEmit",
    "test": "echo \"Error: no test specified\""
  }
}
