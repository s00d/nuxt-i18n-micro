{
  "private": true,
  "name": "nuxt-i18n-next-playground",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate:en": "nuxi generate --dotenv .env.en",
    "generate:de": "nuxi generate --dotenv .env.de",
    "client:prepare": "nuxt prepare",
    "generate": "nuxi generate",
    "init": "nuxt prepare",
    "typecheck": "tsc --noEmit",
    "test:types": "vue-tsc --noEmit",
    "test": "echo \"Error: no test specified\""
  },
  "dependencies": {
    "nuxt": "catalog:"
  }
}
