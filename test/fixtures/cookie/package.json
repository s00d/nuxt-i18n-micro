{
  "private": true,
  "name": "cookie",
  "type": "module",
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "init": "nuxt prepare",
    "typecheck": "tsc --noEmit",
    "test:types": "vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "3.18.1"
  }
}
