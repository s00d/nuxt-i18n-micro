{
  "extends": "./.nuxt/tsconfig.json",
  "exclude": [
    "dist",
    "node_modules",
    "playground",
    // Standalone app: its types come from its own .nuxt once the module is installed
    // from a tarball or npm, so it is not part of this repo's TS project.
    "test/deploy-smoke",
    "scripts",
  ],

}
