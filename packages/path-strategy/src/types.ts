/**
 * Re-export path-strategy types for the "./types" subpath.
 * Use: import type { PathStrategyContext } from '@i18n-micro/path-strategy/types'
 */
export type {
  HreflangTag,
  PathStrategy,
  PathStrategyContext,
  ResolvedRouteLike,
  RouteLike,
  RouterAdapter,
  SeoAttributes,
  SwitchLocaleOptions,
} from './core/types'
